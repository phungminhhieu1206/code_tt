#include <bits/stdc++.h>
using namespace std;

/*
    - Tối ưu để O(N)
*/

int main() {

    while (true) {
        long long n;
        cin >> n;
        if (n == 0) return 0;
        long long a[n];
        for (int i=0; i<n; i++)
            cin >> a[i];

        long long result = 0;

        /* Tính diện tích max ở đây */

    }

    return 0;
}
